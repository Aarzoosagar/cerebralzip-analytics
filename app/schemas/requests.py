"""Pydantic request models for the FastAPI layer.

Step 0 scope: placeholder only. The /query and dashboard endpoints (and
their request bodies) are not implemented yet — only GET /health exists,
which needs no request model.
"""

# Intentionally empty for Step 0.
